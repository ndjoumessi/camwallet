{
  "name": "camwallet",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd backend && npm ci && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma migrate deploy && node dist/main"
  }
}
